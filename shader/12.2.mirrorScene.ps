#version 330 core

in vec2 TexCoords;
out vec4 color;

uniform sampler2D texture1;
//uniform vec3 mirrorColor;

void main()
{
	vec3 mirrorColor = vec3(1, 0, 0);
	vec4 tmp = texture(texture1, TexCoords);
	float ratio = 0.8;
    color = ratio * tmp + (1 - ratio) * vec4(mirrorColor, 1);
}